[build]
  base    = "frontend"
  command = "npm run build"
  publish = "dist"

# React Router: redirect all routes to index.html (SPA fallback)
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
